// Clase 7

let x, y; // Se pueden crear varias variables dentro de una misma línea
x = 17, y = 21; // Se puede hacer asignación de varias variables dentro de una misma línea

let z = x + y; // Se asigna el valor de la operación
console.log(z);

let $1 = 45; // No se puede iniciar las variables con números pero si con un simbolo ($, _)
let _break = 'Rompe'; // No se puede usar palabras reservadas del lenguaje ("break")

console.log($1);
console.log(_break);
